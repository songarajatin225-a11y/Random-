# Optional background music

Put a single track here named:

```
raksha-bandhan.mp3
```

The floating ♪ button appears only when this file exists — no file, no button,
and everything else works exactly the same. Music never autoplays.

Change the filename or volume in `src/data/siteConfig.js` under `music`.
